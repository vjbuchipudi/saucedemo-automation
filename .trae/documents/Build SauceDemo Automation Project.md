I will build the project using Maven as described in the `README.md`.

1.  **Run Maven Build**: Execute `mvn clean install` to clean the project, download dependencies, compile the code, and run the included tests.
    *   This will trigger the Selenium tests, which will launch a Chrome browser to execute the scenarios defined in `Shopping.feature`.
